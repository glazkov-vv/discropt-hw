# Попытка 1. Генетический алгоритм. 

Ищем ответ в виде оптимальной перестановки, после чего красим жадно.

Исходно генерируем семейство случайных перестановок, после этого на каждой итерации разбиваем популяцию по парам, каждая из которых производит перестановку-потомка (естественным образом, упоядочиваем элементы по сумме позиций в двух перестановках). Прореживаем популяцию до ее исходного размера, причем половину элементов убиваем с конца, а половину - случайным образом (элита иммунна от убийств).

Такой подход позволяет пройти все легкие тесты, кроме одного.

Чтобы продвинуться дальше, была добавлена эвристика, которая вместо разбиения на пары случайным образом пытается максимизировать "непохожесть" элементов в паре (жадно набираем к элементу максимально непохожий)

Такой подход позволил пройти все легкие и один сложный тест.

# Попытка 2. 

Реализуем алгоритм DSatur для получения начального приближения.
Далее пытаемся перебором избавиться от цветов, если не получается, модифицируем граф с помощью Kempe chains.

Сильно не улучшилось, прошло еще 1 сложный тест

# Попытка 3. TabuSearch.

Реализован алгоритм TabuSearch: после такого же начального приближения перекрашиваем вершины одного из цветов случайно и далее перекрашиваем по одной вершине пытаясь уменьшить количество конфиликтов (одноцветных пар), запрещая испробованные пары (v, c) на некоторое число итераций.

Это, вместе с небольшими классическими трюками (типа разрешения игнорировать табу при достижении глобального оптимума) позволило пройти все сложные тесты

# Текущие результаты

```
On test case ./data/gc_50_3 got 6 colors. Hard limit passed. Score 7
On test case ./data/gc_70_7 got 18 colors. Easy limit passed. Score 5
On test case ./data/gc_100_5 got 17 colors. Easy limit passed. Score 5
On test case ./data/gc_250_9 got 90 colors. Easy limit passed. Score 5
On test case ./data/gc_500_1 got 18 colors. Easy limit passed. Score 5
On test case ./data/gc_1000_5 got 122 colors. Easy limit passed. Score 5
```